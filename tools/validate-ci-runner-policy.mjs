import fs from 'node:fs'; import path from 'node:path';
const dir='.github/workflows'; const files=fs.readdirSync(dir).filter(x=>x.endsWith('.yml')||x.endsWith('.yaml')); const fail=[];
for(const f of files){const t=fs.readFileSync(path.join(dir,f),'utf8'); if(/^[ \t]*runs-on:[ \t]*(?:self-hosted|\[[^\n\]]*self-hosted)/im.test(t))fail.push(`${f}: self-hosted forbidden`); if(/\$\{\{\s*secrets\./.test(t)&&['alpha2-r1-trusted-edge-signing.yml','alpha2-r2-owned-device-campaign.yml','alpha2-od0-owned-device-harness.yml'].includes(f))fail.push(`${f}: secrets forbidden`);}
const req={
'alpha2-r1-trusted-edge-signing.yml':['0.2.0-alpha.2+2012','FinanceSensor-ALPHA2-R1-TRUSTED-EDGE-BUNDLE-v12.zip','R1_TRUSTED_EDGE_SIGNING=PENDING_USER_TRUSTED_EDGE','OWNED_DEVICE_UAT_REQUEST_ALLOWED=NO','PUBLIC_CI_ORIGINATED_PHYSICAL_PASS=0'],
'alpha2-r2-owned-device-campaign.yml':['0.2.0-alpha.2+2012','R2_PHYSICAL_CAMPAIGN=BLOCKED_BY_R1_SIGNING','OWNED_DEVICE_UAT_REQUEST_ALLOWED=NO','REAL_GMAIL_IN_CI=0','PHYSICAL_ALPHA2_PASS=NO'],
'alpha2-od0-owned-device-harness.yml':['0.2.0-alpha.2+2012','OD0_EXECUTION_ALLOWED=NO','ADB_EXECUTED_BY_CI=0','DEVICE_TOUCHED_BY_CI=0','BUILD_READY=NO']};
for(const [f,markers] of Object.entries(req)){const t=fs.readFileSync(path.join(dir,f),'utf8'); for(const m of markers)if(!t.includes(m))fail.push(`${f}: missing ${m}`); if(/PHYSICAL_ALPHA2_PASS=YES|BUILD_READY=YES|RELEASE_READY=YES|PUBLIC_CI_ORIGINATED_PHYSICAL_PASS=1/.test(t))fail.push(`${f}: forbidden promotion`);}
if(fail.length){console.error('FINANCESENSOR_CI_RUNNER_POLICY=FAIL'); for(const x of fail)console.error('- '+x); process.exit(1);} console.log('FINANCESENSOR_CI_RUNNER_POLICY=PASS'); console.log(`WORKFLOWS_SCANNED=${files.length}`); console.log('ACTIVE_SELF_HOSTED_PATHS=0'); console.log('ALPHA2_2012_CANONICAL_HANDOFF=ROUTED'); console.log('R1_V12_PUBLIC_BUNDLE_GENERATION=ROUTED'); console.log('R2_CURRENT_PHYSICAL_CAMPAIGN=BLOCKED_BY_R1_SIGNING'); console.log('PUBLIC_CI_ORIGINATED_PHYSICAL_PASS=0'); console.log('BUILD_READY=NO');
