import fs from 'node:fs';
const g=JSON.parse(fs.readFileSync('graph/alpha2-human-intervention-gate.json','utf8'));
const r=JSON.parse(fs.readFileSync('graph/physical-receipts/ALPHA2-R1-TRUSTED-EDGE-SIGNING-2012-2026-09-15.json','utf8'));
const fail=m=>{throw new Error(`ALPHA2_HUMAN_GATE_FAILED:${m}`)};
const a=(c,m)=>{if(!c)fail(m)};
const signed='05ee3efd70bb07fe11bde6a21140c03de3ffa872b5f90e7a1014b5106b4b3000';
a(g.schemaVersion==='A2_HUMAN_INTERVENTION_GATE_V3','schema');
a(g.currentCandidate==='0.2.0-alpha.2+2012','candidate');
a(g.candidateCanonicalCommit==='b75cc39318ee749a1123971f19d895d71e35bd91','canonical commit');
a(g.candidateState==='STABLE_SIGNED_UAT_ELIGIBLE','state');
a(g.preSigning?.completed===true && g.preSigning?.requestAllowed===false,'signing frontier must be closed');
a(g.ownedDeviceUat?.requestAllowed===true && g.ownedDeviceUat?.humanUatEligible===true,'UAT must be eligible');
a(g.ownedDeviceUat?.trustedEdgeSigningPass===true && g.ownedDeviceUat?.stableSignedArtifactIdentityFrozen===true,'stable signing proof');
a(g.ownedDeviceUat?.signedApkSha256===signed && g.ownedDeviceUat?.signedApkBytes===182563366,'signed identity');
a(g.ownedDeviceUat?.nextGate==='OD0_SIGNED_APK_INSTALL_AND_LAUNCH','next gate');
a(r.trustedEdgeSigningPass===true && r.signedApkSha256===signed && r.sanitizationPass===true,'receipt');
a(g.claims?.signingRequestAllowed===false && g.claims?.trustedEdgeSigningPass===true,'signing claim');
a(g.claims?.ownedDeviceUatRequestAllowed===true && g.claims?.humanUatEligible===true,'UAT claim');
a(g.claims?.physicalAlpha2Pass===false && g.claims?.buildReady===false && g.claims?.releaseReady===false,'readiness premature');
a((g.blockers??[]).includes('OD0_OWNED_DEVICE_EXECUTION_REQUIRED'),'OD0 blocker absent');
console.log('ALPHA2_HUMAN_INTERVENTION_GATE=PASS');
console.log('CANDIDATE=0.2.0-alpha.2+2012');
console.log('TRUSTED_EDGE_SIGNING_PASS=YES');
console.log(`SIGNED_APK_SHA256=${signed}`);
console.log('SIGNING_REQUEST_ALLOWED=NO');
console.log('OWNED_DEVICE_UAT_REQUEST_ALLOWED=YES');
console.log('NEXT_GATE=OD0_SIGNED_APK_INSTALL_AND_LAUNCH');
console.log('PHYSICAL_ALPHA2_PASS=NO');
console.log('BUILD_READY=NO');
console.log('RELEASE_READY=NO');
