name: financesensor_mobile_shell
description: Synthetic mobile-first FinanceSensor product shell for MK0 UX validation.
publish_to: none
version: 0.0.1+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
